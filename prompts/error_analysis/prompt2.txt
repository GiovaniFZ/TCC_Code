Estou recebendo um erro “Cannot read property ‘map’ of undefined” em React. Como resolver?

```js
export default function ProductList({ products }) {
  return (
    <ul>
      {products.map((p) => (
        <li key={p.id}>{p.name}</li>
      ))}
    </ul>
  );
}
```